import Button from './components/Button';

const App = () => {
  return (
    <div>
      <Button variant="primary">Primary</Button>
      <Button>Default</Button>
    </div>
  );
};

export default App;
